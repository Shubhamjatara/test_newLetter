// // Action Types
// export const SWITCH_CREDENTIALS = "SWITCH_CREDENTIALS";

// Action Creators
export const SWITCH_CREDENTIALS_COMPONENT = (action: Record<string, any>) => ({
  type: action.type,
  payload: action.payload,
});
